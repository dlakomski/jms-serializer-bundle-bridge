services:
    public_message_serializer:
        alias: simple_bus.asynchronous.message_serializer
        public: true

    # needed to make JMSSerializerBundle work
    translator:
        class: stdClass
